{
  "openapi": "3.0.0",
  "info": {
    "title": "Web Agent API",
    "description": "API for searching and parsing web content",
    "version": "1.0.0"
  },
  "paths": {
    "/api/web/search": {
      "post": {
        "summary": "Search web content",
        "description": "Searches the web for content based on query",
        "operationId": "searchWebContent",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "query": {
                    "type": "string",
                    "description": "Search query to find web content"
                  },
                  "operationId": {
                    "type": "string",
                    "description": "operationId of the api without api path and method"
                  }
                },
                "required": ["query", "operationId"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful operation",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "List of parsed web content"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    }
  }
}
